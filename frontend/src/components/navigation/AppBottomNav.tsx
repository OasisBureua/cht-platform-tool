/**
 * Legacy hook point — mobile navigation uses slide-reveal drawer in Layout (`md:hidden`).
 * Export retained so imports stay stable.
 */
export default function AppBottomNav() {
  return null;
}
